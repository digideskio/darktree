<div id="decks">
    <div id="decks-inner" class="container">
        <%= render "shared/alert" %>
        <% if @decks.present? %>
            <h2>All decks</h2>
            <% @decks.each do |deck| %>
                <%= link_to deck_cards_path(deck.id), class: 'deck' do %>
                    <div class="deck-main">
                        <span class="deck-name">
                            <%= deck.name %>
                        </span>
                        <span class="deck-info">
                            <%= "#{deck.cards.count}cards" %>
                        </span>
                    </div>
                    <div class="deck-link">
                        <i class="fa fa-chevron-right" aria-hidden="true"></i>
                    </div>
                <% end %>
            <% end %>
        <% else %>
            <div id="no-deck">
                <i class="fa fa-exclamation-circle" aria-hidden="true"></i>
                <h3>There is no deck!</h3>
                <ol>
                    <!-- 最初にデッキソースを登録しましょう。 -->
                    <!-- デッキソースを登録したら、同期ボタンをクリックしましょう。デッキソースからカードが取り込まれます。 -->
                    <!-- デッキが作られたら学習を開始しましょう。 -->
                    <li>First, register <span class="bold">deck source</span> from <%= link_to 'here', deck_sources_path %>.</li>
                    <li>When you registered deck source, push the sync button. Then cards will be imported from deck source.</li>
                    <li>Now deck is created. Let's begin the learning!</li>
                </ol>
            </div>
        <% end %>
    </div>
</div>
